type UpdateProfileResponse {
  ok: Boolean!
  error: String
}

type Mutation {
  UpdateProfile(
    firstName: String
    lastName: String
    profilePhoto: String
    email: String
    password: String
    age: Int
  ): UpdateProfileResponse!
}
